import ProfileClient from "@/app/components/profile/ProfileClient";

export const metadata = {
  title: "Profile | PinHaoYun",
};

export default function ProfilePage() {
  return <ProfileClient />;
}
